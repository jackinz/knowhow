20190821
1，image（镜像），可以从https://hub.docker.com/上面pull
2，contain（容器）
3，.pd（模型）

docker的管理工具：minikube

20190811
Hypervisor：apt-get，yum
Docker：docker pull

Docker是Hypervisor的替代品，SSH是FTP的替代品，Nginx是Apache的替代品，Git是Subversion的替代品

docker的作用：
1，将软件和依赖环境打包在一起
2，对cpu，内存等资源可以划分上限
3，快速创建删除